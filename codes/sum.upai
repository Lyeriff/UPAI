_hash:
	MOV RT,R1
	SHR R1,#16
	XOR R1,RT
	MUL R1,#73244475

	MOV RT,R1
	SHR R1,#16
	XOR R1,RT
	MUL R1,#73244475
	
	MOV RT,R1
	SHR R1,#16
	XOR R1,RT

	RET
_main:
	MOV R1,#100
	MOV R2,#200
	MOVS RE,%endl%
	CALL _hash
	RPRINT R1
	END